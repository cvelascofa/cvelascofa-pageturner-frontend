import { UserChallengeWithBadge } from './user-challenge-with-badge.model';

describe('UserChallengeWithBadge', () => {
  it('should create an instance', () => {
    expect(new UserChallengeWithBadge()).toBeTruthy();
  });
});
