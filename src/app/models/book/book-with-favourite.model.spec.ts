import { BookWithFavourite } from './book-with-favourite.model';

describe('BookWithFavourite', () => {
  it('should create an instance', () => {
    expect(new BookWithFavourite()).toBeTruthy();
  });
});
