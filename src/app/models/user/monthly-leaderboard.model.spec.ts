import { MonthlyLeaderboard } from './monthly-leaderboard.model';

describe('MonthlyLeaderboard', () => {
  it('should create an instance', () => {
    expect(new MonthlyLeaderboard()).toBeTruthy();
  });
});
